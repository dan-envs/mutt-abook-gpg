## Abook setup
set query_command = "abook --mutt-query '%s'"
macro index,pager a "<pipe-message>abook --add-email-quiet<return>" "Add this sender to Abook"
bind editor <tab> complete-query

# MACRO 'A': Press <SHIFT>+a to update all keys of addressbook contacts
macro index,pager A "<shell-escape>abook --mutt-query ' ' | ~/.abook/get_new_keys.sh && echo 'Press any key to continue...' && read -n 1 -s<enter>" 'Update public keys from contacts in addressbook.'
